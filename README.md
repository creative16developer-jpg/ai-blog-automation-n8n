# ai-blog-automation-n8n
An automated content publishing system that generates SEO-optimized blog posts using AI and publishes them directly to WordPress. The workflow pulls keywords from Google Sheets, generates content via AI API, posts to WordPress using REST API, and updates the sheet status automatically.
